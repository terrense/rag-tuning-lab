# Put Your Documents Here

把你想检索的资料放到这个目录，下一次运行 ingest 会自动加入知识库。

支持格式：

- `.pdf`
- `.txt`
- `.md`
- `.markdown`

建议：

- 文件名尽量有意义，例如 `milvus_index_notes.pdf`、`rag_chunking.md`。
- PDF 尽量选择可复制文字的版本；扫描版图片 PDF 暂时不会 OCR。
- 先放 3-10 个文件练调参，等流程熟了再放大量资料。
- 这个目录默认被 `.gitignore` 忽略，避免把私人资料提交进 git。
